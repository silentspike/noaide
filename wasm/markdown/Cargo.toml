[package]
name = "markdown-wasm"
version = "0.1.0"
edition = "2024"
description = "WASM Markdown renderer for noaide"
license = "MIT"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
wasm-bindgen = "0.2"
pulldown-cmark = "0.13"
