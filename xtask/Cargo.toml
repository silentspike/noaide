[package]
name = "xtask"
version = "0.1.0"
edition = "2021"
description = "Build automation for noaide (eBPF compilation)"
license = "MIT"

[dependencies]
clap = { version = "4", features = ["derive"] }
serde_json = "1"
